---
// src/components/Hero.astro
import { Image } from 'astro:assets';
import heroImage from '@images/imagen-codigo.jpg';
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-linear-to-br from-blue-50 via-white to-purple-50">
  
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-20 left-10 w-72 h-72 bg-blue-400 rounded-full mix-blend-multiply filter blur-xl opacity-20 animate-blob"></div>
    <div class="absolute bottom-20 right-10 w-72 h-72 bg-purple-400 rounded-full mix-blend-multiply filter blur-xl opacity-20 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      
      <div class="opacity-0 animate-fade-in-right"> <div class="flex items-center gap-2 mb-6">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-blue-600">
            <path d="m18 16 4-4-4-4"/><path d="m6 8-4 4 4 4"/><path d="m14.5 4-5 16"/>
          </svg>
          <span class="text-blue-600 font-semibold">Axios Technologies</span>
        </div>
        
        <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold text-gray-900 mb-6">
          Transformamos tus{' '}
          <span class="text-transparent bg-clip-text bg-linear-to-r from-blue-600 to-purple-600">
            ideas digitales
          </span>
          {' '}en realidad
        </h1>
        
        <p class="text-xl text-gray-600 mb-8">
          Creamos páginas web modernas, rápidas y adaptadas a tus necesidades. 
          Tu presencia digital comienza aquí.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4">
          <button 
            id="btn-contact"
            class="px-8 py-4 bg-linear-to-r from-blue-600 to-purple-600 text-white rounded-lg font-semibold flex items-center justify-center gap-2 hover:shadow-xl hover:scale-105 active:scale-95 transition-all duration-300"
          >
            Comenzar Proyecto
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>
            </svg>
          </button>
          
          <button 
            id="btn-services"
            class="px-8 py-4 border-2 border-gray-300 text-gray-700 rounded-lg font-semibold hover:border-blue-600 hover:text-blue-600 hover:scale-105 active:scale-95 transition-all duration-300"
          >
            Ver Servicios
          </button>
        </div>
      </div>

      <div class="relative opacity-0 animate-fade-in-left animation-delay-500">
        <div class="relative rounded-2xl overflow-hidden shadow-2xl">
          <Image
            src={heroImage}
            alt="Desarrollo web profesional"
            class="w-full h-auto object-cover"
            loading="eager"
          />
          
          <div class="absolute bottom-8 left-8 bg-white rounded-lg shadow-xl p-6 max-w-xs animate-float-up opacity-0 animation-delay-1000">
            <div class="flex items-center gap-3">
              <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
                <div class="w-6 h-6 bg-green-500 rounded-full animate-pulse" />
              </div>
              <div>
                <p class="font-semibold text-gray-900">100% Satisfacción</p>
                <p class="text-sm text-gray-600">Clientes satisfechos</p>
              </div>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  document.getElementById('btn-contact')?.addEventListener('click', () => {
    document.getElementById('contact')?.scrollIntoView({ behavior: 'smooth' });
  });

  document.getElementById('btn-services')?.addEventListener('click', () => {
    document.getElementById('services')?.scrollIntoView({ behavior: 'smooth' });
  });
</script>

<style>
  @keyframes blob {
    0% { transform: translate(0px, 0px) scale(1); }
    33% { transform: translate(30px, -50px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
    100% { transform: translate(0px, 0px) scale(1); }
  }
  .animate-blob {
    animation: blob 7s infinite;
  }
  .animation-delay-2000 {
    animation-delay: 2s;
  }
  
  @keyframes fadeInRight {
    from { opacity: 0; transform: translateX(-50px); }
    to { opacity: 1; transform: translateX(0); }
  }
  .animate-fade-in-right {
    animation: fadeInRight 0.8s ease-out forwards;
  }

  @keyframes fadeInLeft {
    from { opacity: 0; transform: translateX(50px); }
    to { opacity: 1; transform: translateX(0); }
  }
  .animate-fade-in-left {
    animation: fadeInLeft 0.8s ease-out forwards;
  }

  @keyframes floatUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-float-up {
    animation: floatUp 0.5s ease-out forwards;
  }
  
  .animation-delay-500 { animation-delay: 0.5s; }
  .animation-delay-1000 { animation-delay: 1s; }
</style>