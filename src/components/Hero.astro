---
// src/components/Hero.astro
import { Image } from 'astro:assets';
import heroImage from '@images/imagen-codigo.jpg';
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-linear-to-br">
  
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute rounded-full bg-primary top-20 left-10 w-72 h-72 mix-blend-multiply filter blur-xl opacity-20 animate-blob"></div>
    <div class="absolute rounded-full bg-secondary bottom-20 right-10 w-72 h-72 mix-blend-multiply filter blur-xl opacity-20 animate-blob animation-delay-2000"></div>
  </div>

  <div class="relative px-4 py-20 mx-auto max-w-7xl sm:px-6 lg:px-8">
    <div class="grid items-center gap-12 md:grid-cols-2">
      
      <div class="opacity-0 animate-fade-in-right"> <div class="flex items-center gap-2 mb-6">
          <!-- <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-primary">
            <path d="m18 16 4-4-4-4"/><path d="m6 8-4 4 4 4"/><path d="m14.5 4-5 16"/>
          </svg> -->
          <h3 class="font-semibold text-primary">Axios Technologies</h3>
        </div>
        
        <h1 class="mb-6 text-5xl font-bold text-gray-900 md:text-6xl lg:text-7xl">
          Transformamos tus{' '}
          <span class="text-transparent bg-clip-text bg-linear-to-r from-primary via-primary to-secondary">
            ideas digitales
          </span>
          {' '}en realidad
        </h1>
        
        <p class="mb-8 text-xl text-gray-600">
          Creamos páginas web modernas, rápidas y adaptadas a tus necesidades. 
          Tu presencia digital comienza aquí.
        </p>
        
        <div class="flex flex-col gap-4 sm:flex-row">
          <button 
            id="hero-btn-contact"
            class="flex items-center justify-center gap-2 px-8 py-4 font-semibold text-white transition-all duration-300 rounded-lg bg-secondary hover:shadow-xl hover:scale-105 active:scale-95"
          >
            Comenzar Proyecto
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>
            </svg>
          </button>
          
          <button 
            id="hero-btn-services"
            class="px-8 py-4 font-semibold text-primary transition-all duration-300 border-2 border-primary-300 rounded-lg hover:border-secondary hover:text-secondary hover:scale-105 active:scale-95"
          >
            Ver Servicios
          </button>
        </div>
      </div>

      <div class="relative opacity-0 animate-fade-in-left animation-delay-500">
        <div class="relative overflow-hidden shadow-2xl rounded-2xl">
          <Image
            src={heroImage}
            alt="Desarrollo web profesional"
            class="object-cover w-full h-auto"
            loading="eager"
          />
          
          <div class="absolute max-w-xs p-6 bg-white rounded-lg shadow-xl opacity-0 bottom-8 left-8 animate-float-up animation-delay-1000">
            <div class="flex items-center gap-3">
              <div class="flex items-center justify-center w-12 h-12 bg-green-100 rounded-full">
                <div class="w-6 h-6 bg-green-500 rounded-full animate-pulse" />
              </div>
              <div>
                <p class="font-semibold text-gray-900">100% Satisfacción</p>
                <p class="text-sm text-gray-600">Clientes satisfechos</p>
              </div>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  document.getElementById('hero-btn-contact')?.addEventListener('click', () => {
    document.getElementById('contact')?.scrollIntoView({ behavior: 'smooth' });
  });

  document.getElementById('hero-btn-services')?.addEventListener('click', () => {
    document.getElementById('services')?.scrollIntoView({ behavior: 'smooth' });
  });
</script>

<style>
  @keyframes blob {
    0% { transform: translate(0px, 0px) scale(1); }
    33% { transform: translate(30px, -50px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
    100% { transform: translate(0px, 0px) scale(1); }
  }
  .animate-blob {
    animation: blob 7s infinite;
  }
  .animation-delay-2000 {
    animation-delay: 2s;
  }
  
  @keyframes fadeInRight {
    from { opacity: 0; transform: translateX(-50px); }
    to { opacity: 1; transform: translateX(0); }
  }
  .animate-fade-in-right {
    animation: fadeInRight 0.8s ease-out forwards;
  }

  @keyframes fadeInLeft {
    from { opacity: 0; transform: translateX(50px); }
    to { opacity: 1; transform: translateX(0); }
  }
  .animate-fade-in-left {
    animation: fadeInLeft 0.8s ease-out forwards;
  }

  @keyframes floatUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-float-up {
    animation: floatUp 0.5s ease-out forwards;
  }
  
  .animation-delay-500 { animation-delay: 0.5s; }
  .animation-delay-1000 { animation-delay: 1s; }
</style>